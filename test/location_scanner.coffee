describe 'LocationScanner', ->

  it 'creates a Bancroft instance'

  it 'handles new location events'

  it 'triggers new location events only when location has change'

  it 'keeps track of idle time'
